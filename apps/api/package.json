{
  "name": "@kevins-nest/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step for Python'",
    "lint": "echo 'Add linter if needed'"
  }
}